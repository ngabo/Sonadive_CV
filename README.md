# Sonadive CV Generator

Automatic CV generator with the Sonadive template. Fill the form on the left, get a live A4 preview on the right, and export to PDF via the Download PDF button (print to PDF, margins: none).

Single self-contained HTML page - no build, no dependencies. Data is saved locally in the browser (localStorage).
